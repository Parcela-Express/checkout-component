/*
 * Parcela Express API
 * Parcela Express API
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.30
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {CustomerDTO} from './CustomerDTO';

/**
 * The CreatePaymentDto model module.
 * @module model/CreatePaymentDto
 * @version 1.0
 */
export class CreatePaymentDto {
  /**
   * Constructs a new <code>CreatePaymentDto</code>.
   * @alias module:model/CreatePaymentDto
   * @class
   * @param amountCents {Number} 
   * @param description {String} 
   * @param cardAttributes {Object} 
   * @param installmentPlan {Object} 
   * @param customer {module:model/CustomerDTO} 
   */
  constructor(amountCents, description, cardAttributes, installmentPlan, customer) {
    this.amountCents = amountCents;
    this.description = description;
    this.cardAttributes = cardAttributes;
    this.installmentPlan = installmentPlan;
    this.customer = customer;
  }

  /**
   * Constructs a <code>CreatePaymentDto</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/CreatePaymentDto} obj Optional instance to populate.
   * @return {module:model/CreatePaymentDto} The populated <code>CreatePaymentDto</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new CreatePaymentDto();
      if (data.hasOwnProperty('amount_cents'))
        obj.amountCents = ApiClient.convertToType(data['amount_cents'], 'Number');
      if (data.hasOwnProperty('description'))
        obj.description = ApiClient.convertToType(data['description'], 'String');
      if (data.hasOwnProperty('card_attributes'))
        obj.cardAttributes = ApiClient.convertToType(data['card_attributes'], Object);
      if (data.hasOwnProperty('installment_plan'))
        obj.installmentPlan = ApiClient.convertToType(data['installment_plan'], Object);
      if (data.hasOwnProperty('customer'))
        obj.customer = CustomerDTO.constructFromObject(data['customer']);
      if (data.hasOwnProperty('form_payment'))
        obj.formPayment = ApiClient.convertToType(data['form_payment'], 'String');
      if (data.hasOwnProperty('sale_id'))
        obj.saleId = ApiClient.convertToType(data['sale_id'], 'String');
      if (data.hasOwnProperty('pre_capture'))
        obj.preCapture = ApiClient.convertToType(data['pre_capture'], 'Boolean');
      if (data.hasOwnProperty('protest_link'))
        obj.protestLink = ApiClient.convertToType(data['protest_link'], 'Boolean');
      if (data.hasOwnProperty('amount_fee'))
        obj.amountFee = ApiClient.convertToType(data['amount_fee'], 'Number');
    }
    return obj;
  }
}

/**
 * @member {Number} amountCents
 */
CreatePaymentDto.prototype.amountCents = undefined;

/**
 * @member {String} description
 */
CreatePaymentDto.prototype.description = undefined;

/**
 * @member {Object} cardAttributes
 */
CreatePaymentDto.prototype.cardAttributes = undefined;

/**
 * @member {Object} installmentPlan
 */
CreatePaymentDto.prototype.installmentPlan = undefined;

/**
 * @member {module:model/CustomerDTO} customer
 */
CreatePaymentDto.prototype.customer = undefined;

/**
 * Allowed values for the <code>formPayment</code> property.
 * @enum {String}
 * @readonly
 */
CreatePaymentDto.FormPaymentEnum = {
  /**
   * value: "credit"
   * @const
   */
  credit: "credit",

  /**
   * value: "debit"
   * @const
   */
  debit: "debit",

  /**
   * value: "billet"
   * @const
   */
  billet: "billet",

  /**
   * value: "pix"
   * @const
   */
  pix: "pix",

  /**
   * value: "pending"
   * @const
   */
  pending: "pending"
};
/**
 * @member {module:model/CreatePaymentDto.FormPaymentEnum} formPayment
 */
CreatePaymentDto.prototype.formPayment = undefined;

/**
 * @member {String} saleId
 */
CreatePaymentDto.prototype.saleId = undefined;

/**
 * @member {Boolean} preCapture
 */
CreatePaymentDto.prototype.preCapture = undefined;

/**
 * @member {Boolean} protestLink
 */
CreatePaymentDto.prototype.protestLink = undefined;

/**
 * @member {Number} amountFee
 */
CreatePaymentDto.prototype.amountFee = undefined;

